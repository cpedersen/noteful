import React, { Component } from 'react';
import './header.css';

function Header () {
    return (
        <div className="Header">
            <h1 className="HeadingText">Noteful</h1>
        </div>
    );
}

export default Header;
